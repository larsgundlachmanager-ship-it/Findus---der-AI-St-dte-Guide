/**
 * Fremdwort-IPA — DEAKTIVIERT (Hard-Reboot).
 * Cartesia language:de + native Alina/Sebastian: kein Mid-Sentence-IPA.
 */

export function ipaToCartesiaSpan(_ipa: string): string {
  return '';
}

export function resetForeignCartesiaPronunciationCache(): void {}

export function applyForeignCartesiaPronunciations(text: string): string {
  return text;
}
