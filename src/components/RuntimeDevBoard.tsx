/**
 * Runtime Dev-Board — bewusst aus dem HUD entfernt (Kosten-Chip störte).
 * Stub bleibt exportiert, damit Fast-Refresh/Soft-Reload nicht crasht.
 * Kosten: Settings → Entwickler → Kosten-Übersicht.
 */

import React from 'react';

export function RuntimeDevBoard(): null {
  return null;
}
