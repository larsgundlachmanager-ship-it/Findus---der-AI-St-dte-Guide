/**
 * Draft bar stub — planning engine removed; calendar UI stays.
 * Kept so existing imports do not break; renders nothing.
 */

import React from 'react';

export function PlanningDraftBar() {
  return null;
}
