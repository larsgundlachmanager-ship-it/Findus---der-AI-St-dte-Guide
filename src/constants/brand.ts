/**
 * Öffentlicher Markenname (UI, TTS-Prompts, Store-Label).
 * Deep-Links: `yorro://` + Legacy `findus://`. Package bleibt `de.findus.app`.
 */
export const APP_BRAND_NAME = 'Yorro' as const;

/** Kurz für „Frag doch …“ / Wake-Anrede */
export const APP_BRAND_WAKE = 'Yorro' as const;

/** Öffentliche Website (Legal, Share, OG). */
export const APP_WEBSITE_URL = 'https://yorro.de/' as const;
